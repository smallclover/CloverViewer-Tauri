# 更新日志

本文件记录 CloverViewer-Tauri 每个发布版本的用户可见变化。
GitHub Release 页面（v0.1.2 起）直接使用对应版本的正文内容。

版本号规则：[语义化版本](https://semver.org/lang/zh-CN/)；本项目为桌面应用，`0.x` 阶段
以「功能新增」为主的版本号进位仍按 minor 处理，补丁版本只放修复。

本文件只记录**已发布**的版本；尚未发布的改动不进这里，发布时再按版本补写段落。
发布流水线按 `## v<版本号>` 抽取对应段落作为 Release 正文，所以发版前必须先写好该段落。

---

## v0.1.12 — 图片编辑与查看体验优化

### 新增

*   **图片编辑工作流**：可在查看器中进入编辑模式，对图片进行标注和马赛克处理；编辑历史支持撤销与重做，并可将结果另存为新图片。
*   **更丰富的图片详情浏览**：查看器新增图片预览条与更完整的属性信息，支持按名称、日期、类型和大小排序。

### 改进与修复

*   **查看器导航更简洁**：精简不必要的导航操作，使文件夹和图片间的浏览路径更清晰。
*   **界面密度适配显示器**：主查看器和截图覆盖窗会根据当前显示器的逻辑分辨率自动调整页面缩放；2K 及以上保持原有密度，较小屏幕可容纳更多内容。
*   **更新检查不会无声卡住**：连接更新源超时后会提示检查网络或代理，下载安装请求同样设有超时保护。

### 文档

*   补充按显示器分辨率调整界面密度的实现说明。

---

## v0.1.11 — 局域网分享与图库浏览优化

### 新增

*   **局域网图片分享**：在图片查看器的右键菜单或截图标注工具中创建临时分享链接；同一局域网内的设备可扫码或打开链接预览并下载图片，无需上传到第三方服务。
*   **可控的分享有效期与下载规则**：在设置中选择默认有效期（1 分钟至 1 小时），并设为有效期内不限下载或下载一次即失效；可随时手动停止分享。

### 改进与修复

*   **图库浏览更清晰**：缩略图按月份分组，支持在小、中、大密度间直接选择，并提供明确的升序、降序排序菜单和返回上级文件夹入口。
*   **网格布局更适应窗口尺寸**：缩略图会根据可用宽度均匀铺满网格；大目录仍保持虚拟滚动加载。

### 说明

*   局域网分享仅临时监听本机局域网地址，链接含随机令牌；图片只保存在应用内存中，到期、下载限制触发或手动停止后即不再提供。

---

## v0.1.10 — 设置页重构与临时缓存管理

### 新增

*   **设置页改为分类 + 搜索**：左侧按「应用 / 截图 / 维护」分组，包含常规、查看、截图、快捷键、缓存五个分类；顶部搜索框同时过滤分类与设置项，没有匹配项时给出提示，搜索词在每次打开设置时清空。
*   **每项设置带说明文字**：每个设置行显示它的实际作用（例如「应用启动时清理超过保留期的文件」），不必再靠选项名推测行为。
*   **临时截图缓存管理（设置 → 缓存）**：显示 `%TEMP%\CloverViewer` 里临时长截图的文件数与占用体积；可设保留期（不自动清理 / 1 小时 / 1 天 / 7 天 / 30 天）在启动时自动清理，也可点「立即清理」按「超过 1 小时 / 1 天 / 7 天 / 30 天 / 全部」立即释放空间。
*   **标题栏改为菜单栏**：文件（打开文件夹）、编辑（设置）、帮助（关于 CloverViewer）三个下拉菜单，同一时间只展开一个；点击菜单项、点击窗口其他位置或按 `Esc` 都会收起，菜单区域内按住鼠标不会拖动窗口。

### 说明

*   缓存清理只作用于应用自己的临时目录 `%TEMP%\CloverViewer`（即长截图「在查看器中打开」时写入的临时 PNG）。它不扫描系统 Temp 的其他内容，不跟随目录内的符号链接，正在被查看器占用的文件会被跳过。
*   旧配置缺少 `cache_cleanup_after_hours` 字段时按「保留 7 天」处理；选择「不自动清理」则启动时不做任何删除。

### 文档与维护

*   **新增在线介绍页**：<https://smallclover.github.io/CloverViewer-Tauri/>（功能概览、常见问题与下载入口），由 GitHub Actions 自动发布。
*   三语 README 补充「适合谁用」、常见问题与文档索引；修正「启动后自动检查更新」的旧描述 —— 更新检查自 v0.1.8 起完全由用户手动触发。
*   新增 `docs/release.md` 发布流程、`AGENTS.md` 协作约定，以及发版前机械预检 `npm run release:check`。
*   修复 `cargo clippy -D warnings` 下的两处告警（滚动截图实时预览的高度边界改用 `clamp`；自动滚动进度上报去掉多余的布尔参数），`quality` 工作流恢复绿色。

---

## v0.1.9 — 手动长截图重构

### 新增

*   **手动滚动优先**：长截图默认由用户控制滚动位置；自动滚动改为设置中的试验功能，默认关闭。
*   **可信帧实时预览**：捕获过程中区分已验证内容与当前候选帧；候选帧无法可靠匹配时会明确标记为未采用，已拼接内容仍会保留。

### 改进与修复

*   **更可靠的手动拼接**：不再因短暂的不匹配、固定控件或动画自动停止；用户可继续滚动，并在完成时只收取经过验证的新内容，避免重复尾帧。
*   **重构长截图界面**：开始、捕获和结果三种状态统一优先停靠在选区右侧；实时状态采用预览与信息分区，结果页补全质量、尺寸与操作层级。
*   **选区聚焦反馈恢复**：手动捕获也显示呼吸光晕，实际取帧时会临时隐藏，避免光晕混入成图。

### 说明

*   自动滚动仍受目标应用的滚动实现、固定栏和动态内容影响，建议优先使用手动模式。

---

## v0.1.8 — 滚动截图与放大镜修复

### 改进与修复

*   **更干净的滚动截图**：自动识别并移除应用两侧的固定栏、编辑器内部滚动条，以及底部状态栏或横向滚动条，减少长图中的重复界面元素。
*   **更稳定的长图拼接**：忽略滚动到底时的回弹帧，避免旧内容被错误追加到长图末尾；未通过匹配校验的帧也不会再改变拼接基准。
*   **滚动截图时不显示放大镜**：从框选到完成的整个滚动截图流程均不再显示或响应取色放大镜。
*   **放大镜持续跟随鼠标**：鼠标移到截图工具栏、弹出面板等界面元素时，放大镜仍会更新位置，不再停留在上一帧。

### 维护

*   **界面模块化**：拆分截图编辑器、主窗口控制器与多语言资源，保持功能不变的同时降低后续维护和测试成本。

---

## v0.1.7 — 更新提示与滚动截图界面优化

### 新增

*   **可读的更新提示**：手动检查到新版本后，应用内弹窗展示版本号和完整更新日志，可选择立即更新或稍后更新。
*   **滚动截图模式切换**：开始面板支持在自动滚动和手动滚动之间直接切换，并根据模式显示对应说明与操作。

### 改进与修复

*   **滚动截图结果面板重整**：以尺寸、帧数、耗时、预览和主要操作分区展示，窄屏下仍优先保证复制、保存和打开按钮可用。
*   **选区状态更直观**：开始前明确显示选区尺寸及高度是否满足拼接要求，并优化主按钮的悬停对比度。

---

## v0.1.6 — 手动滚动截图体验完善

### 新增

*   **实时手动滚动模式**：框选区域后可按自己的节奏连续滚动，程序按实际画面位移自动取帧、识别重叠并拼接，无需逐段停下确认。

### 改进与修复

*   **取消操作更明确**：按 `Esc` 后立即显示正在收取最后一帧的反馈；完成后控制面板恢复可点击。
*   **避免选区线混入长图**：滚动采集期间不再绘制选区边框，消除成图中的横线和纵线伪影。
*   **统一截图界面鼠标样式**：开始面板、滚动控制面板和结果预览均使用普通指针，不再错误显示十字准星。

### 维护

*   **版本号单一来源**：以根目录 `package.json` 为唯一版本配置；Tauri、Rust 元数据和 About 画面的版本显示均由其引用或同步，README 发布徽章改为自动跟随最新 Release。

---

## v0.1.5 — 自动更新

### 新增

*   **应用内更新**：启动后自动检查 GitHub Release，也可在“设置 → 软件更新”手动检查。
*   **用户确认与进度反馈**：发现新版本时展示版本和更新说明；确认后下载、校验、安装并自动重启。

### 发布与安全

*   **更新包签名校验**：使用 Tauri Updater 的独立签名验证下载包，拒绝未被内置公钥信任的更新。
*   **发布流水线支持**：GitHub Actions 自动上传已签名安装包及 `latest.json` 更新清单。

### 迁移说明

*   从 v0.1.4 升级到 v0.1.5 仍需手动下载安装；从 v0.1.5 起可接收后续自动更新。

---

## v0.1.4 — MCP 屏幕视觉能力与本地接入加固

### 新增

*   **面向视觉 AI 的 MCP 截图工作流**：新增 `list_monitors`、`get_screenshot`、
    `delete_screenshot` 与 `ocr_screenshot`；可先发现显示器、截图后按 `captureId` 重读、
    识别文字，最后显式删除结果。
*   **更灵活的截图目标**：`take_screenshot` 支持活动窗口、指定显示器、全部显示器，以及
    虚拟桌面坐标中的单屏区域；显示器列表提供当前 ID、位置、尺寸、主屏状态与可用缩放信息。
*   **直接交付图片给 AI**：默认返回 MCP PNG 图片内容和结构化元数据，不再要求客户端访问
    服务端本地路径；仍可用 `delivery: "path"` 或 `"both"` 保持本地工作流兼容。
*   **截图生命周期管理**：截图保存到应用本地数据目录，默认保留 24 小时且总量限制为 512 MiB；
    每张图片使用不透明 `captureId`，便于在同一 MCP 会话中继续引用。

### 改进与安全

*   **HTTP MCP 本地访问保护**：HTTP 服务仅绑定回环地址，要求 `--token <secret>`，
    并拒绝非本机 Origin；stdio 模式支持不同 MCP 客户端分别启动独立服务。
*   **更可靠的捕获与返回**：指定显示器只捕获目标屏幕；文件名跨进程唯一且排他创建；
    图片返回宽度与总像素受限，降低多屏截图占用。
*   **OCR 接入 MCP**：可对本服务创建的截图调用现有 Windows OCR（中、英、日），无需启动 GUI。

### 文档与验证

*   更新中、英、日 README 的 MCP 工具、HTTP 鉴权和交付格式说明。
*   新增协议集成测试，覆盖 stdio 握手、工具发现、参数错误、HTTP Token、Origin、会话和工具发现；
    发布前完整测试套件通过。

### 迁移说明

*   通过 HTTP 接入时，现必须显式提供 `--token <secret>`，并在请求中携带
    `Authorization: Bearer <secret>`。仅运行 `--mcp` 的 stdio 客户端无需修改鉴权配置。

---

## v0.1.3 — 界面打磨与截图体验改进

### 改进

*   **主界面体验统一**：完善左侧导航轨、内容区上下文栏与整页设置面板的过渡和空态表现，
    浏览图片与调整设置时的层级更清晰。
*   **截图提示更易读**：快捷键帮助框与滚动截图面板统一视觉样式，并会根据当前鼠标所在屏幕落位。
*   **长截图结果可直接复制**：在查看器中打开长截图后，可通过原生剪贴板复制图片，避免受 WebView
    图片剪贴板兼容性影响。

### 修复

*   **避免滚动截图混入覆盖窗 UI**：当提示面板与捕获区重叠时，优先使用 Windows 捕获排除；
    不支持时整段隐藏提示，避免逐帧闪烁或把 HUD 拼进长图。
*   **稳定首帧与重试帧的捕获时机**：滚动截图在所有采帧路径上都应用同一套 HUD 让位策略。

### 维护

*   清理已完成阶段的路线图、设计草图与重复 Logo 资源，保留可复现的滚动截图验证工具。

---

## v0.1.2 — 滚动截图（长截图）

一句话：**框选一块可滚动区域，程序自动滚下去并把每一屏拼成一张长图。**

### 新增

*   **滚动截图（长截图）**：截图工具栏新增「滚动截图」按钮。
    框选区域后自动滚动、逐帧按重叠像素拼接成长图；结束后可 **复制 / 保存到桌面 / 在查看器中打开**。
*   **专属全局热键 `Alt+Shift+S`（可自定义）**：按一次即进入滚动截图待框选态，
    **框选完自动开始**，单击窗口则直接对该窗口开跑 —— 最短路径是「按热键 → 点一下浏览器」。
*   **自动探测注入方式**：不同应用吃的滚动方式不一样（浏览器吃滚轮消息、资源管理器只认真实滚轮或
    PageDown、标准列表控件吃滚动条命令）。程序在主循环里挨个试、谁先让画面动就用谁，
    并顺手标定「每格滚多少像素」来决定步长，无需用户理解这些差异。
*   **智能拼接**：自动识别并只保留一份**吸顶标题栏 / 吸底工具栏**，自动忽略左右两侧的
    滚动条与固定侧栏；周期性内容（等距列表、图标网格）用「上一步位移」先验消除歧义。
*   **稳定帧等待**：按「画面是否已经停住」而不是固定延迟取帧。Chrome 的平滑滚动要
    ≈1.1s 才停，固定延迟必然截到「滚到一半」的中间帧 —— 这是本项目 P0 实测结论。
*   **实时进度反馈**：HUD 显示已捕获高度、帧数与缩略预览，边框颜色反映匹配状态
    （绿 = 良好 / 琥珀 = 部分帧证据不足 / 红 = 失败）；整屏选区时改用任务栏进度条，
    保证「正在跑」这件事始终可见。
*   **随时停止**：Esc 或 HUD 的停止按钮均可中止，**已捕获部分不丢**（标记为「部分结果」）。
*   **失败可解释**：不可滚动、选区太矮、内容有大面积动画、连续多帧无法匹配等原因分别给出
    对应文案与排查清单，而不是笼统的「截图失败」。
*   **热键冲突自动降级**：`Alt+Shift+S` 被其它程序（微信 / QQ / 输入法 / 别的截图工具）占用时，
    自动改用候选组合并在主窗口提示「想用的 / 实际生效的」分别是哪个。
*   **设置面板新增「滚动截图热键」**：与截图热键、取色热键一致，可改、可即时重注册。
*   **调试开关 `CLOVER_SCROLL_DEBUG=1`**：把每帧的位移、匹配质量、追加决策写入
    临时目录的 `clover_scroll_debug.log`，遇到拼接问题时便于反馈定位。

### 改进

*   **截图覆盖窗状态更稳**：会话语义的临时状态（鼠标穿透、临时全局 Esc、任务栏进度、
    HUD 让位）改为「租约 + 析构兜底」，任何一条错误路径都不会把窗口留在异常状态。
*   **滚动截图与普通截图共用同一把「正在截图」互斥**，长截图过程中不会再被 Alt+S 打断。
*   **「在查看器中打开」不再触发主窗口冷启动**：主窗口若在托盘态会先被唤起再载入长图，
    并提示「已在查看器中打开长图」。

### 修复

*   修复在页面**底部**启动滚动截图时被误报为「该区域没有发生滚动 / 可能不可滚动」的问题。
*   修复会话异常结束时覆盖窗可能**永久停在鼠标穿透态**、且临时全局 Esc 未注销的问题
    （表现为覆盖窗像「死」了一样点不到任何按钮）。
*   修复结果态 HUD 在特定时序下按钮与缩略预览全部不显示的问题。
*   修复实时缩略预览与最终长图相差一个页脚高度、对不上的问题。
*   修复「每步滚动像素」标定完成后反而可能每步只追加一点、帧数暴涨的问题。

### 文档

*   新增 `CHANGELOG.md`（本文件），Release 正文由 CI 从此文件对应的版本段落生成。
*   完成滚动截图的设计、兼容性实测与问题复盘。
    （含发布前 review 抓到的 5 个问题与教训）。
*   三语言 README 补充滚动截图功能说明与快捷键表。
*   新增 `tools/README.md`，说明 `tools/` 下的验证脚本（长页面素材、确定性可滚动目标、
    拼接算式离线复刻、多屏落位几何检查）。

### 说明

*   滚动截图依赖目标应用的渲染稳定性，**不保证 100% 成功**；成功率的边界已写进
    失败时已捕获部分会保留，并会给出排查提示。
*   长图标注与结果态裁剪（视图变换 zoom/pan）留待下一阶段，当前长图在查看器中查看与导出。

---

## v0.1.0 — 首个公开版本

*   图片查看器：网格 / 单图双视图、大目录虚拟滚动、缩略图 LRU 缓存、EXIF 属性、旋转翻转、
    复制图片与路径。
*   多屏截图与标注：虚拟桌面跨屏拼接、矩形 / 椭圆 / 箭头 / 画笔 / 马赛克 / 文字、
    放大镜取色、撤销重做、复制到剪贴板与保存。
*   OCR 文字识别：基于 Windows 原生 UWP OCR（中英日等多语言）。
*   内置 MCP Server：`CloverViewer.exe --mcp`（stdio）与 `--mcp-http`（streamable HTTP）。
*   三语言界面（简体中文 / English / 日本語）、设置面板、开机自启、托盘常驻。
*   NSIS 安装包（currentUser 免 UAC，三语言安装界面）。
