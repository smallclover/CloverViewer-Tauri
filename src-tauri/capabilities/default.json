{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "CloverViewer 主窗口默认权限",
  "windows": ["main", "screenshot"],
  "permissions": [
    "core:default",
    "dialog:default",
    "global-shortcut:default"
  ]
}
